<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Color Toggle Webpage</title>
  <style>
    /* Initial styling for the body */
    body {
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      height: 100vh;
      background-color: #A0D9D9; /* Default pastel teal */
      transition: background-color 0.3s;
      font-family: Arial, sans-serif;
    }

    /* Styling for the title */
    h1 {
      font-size: 24px;
      margin-bottom: 20px;
      color: #555;
    }

    /* Button styling */
    button {
      padding: 10px 20px;
      font-size: 16px;
      cursor: pointer;
      background-color: #A0D9D9; /* Matching teal button */
      border: none;
      border-radius: 5px;
      transition: background-color 0.3s;
    }

    /* Button hover effect */
    button:hover {
      background-color: #88C3C3;
    }
  </style>
</head>
<body>

  <!-- Page title -->
  <h1>Welcome to the Colour Toggle Page</h1>

  <!-- Button to toggle the color -->
  <button onclick="toggleColor()">Toggle Color</button>

  <script>
    // JavaScript function to toggle between pastel teal and light red
    function toggleColor() {
      document.body.style.backgroundColor =
        document.body.style.backgroundColor === 'rgb(160, 217, 217)' ? '#FF9A9E' : '#A0D9D9';
    }
  </script>

</body>
</html>
